[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eye"
version = "0.1.0"
description = "20/20/20 vision rule CLI tool"
requires-python = ">=3.9"
dependencies = ["pyobjc-framework-Cocoa"]

[project.scripts]
eye = "eye.cli:main"
