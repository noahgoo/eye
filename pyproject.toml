[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eye"
version = "0.1.0"
description = "20/20/20 vision rule app"
requires-python = ">=3.9"
dependencies = ["pyobjc-core", "pyobjc-framework-Cocoa"]
